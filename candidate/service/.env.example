# local dev only — matches docker-compose.yml, not a production template
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/orders
FRONTEND_ORIGIN=http://localhost:5173
# small default is intentional for this service's scale — see database-conventions skill before changing
DB_POOL_SIZE=10
